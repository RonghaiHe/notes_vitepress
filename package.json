{
  "devDependencies": {
    "vitepress": "2.0.0-alpha.11",
    "cpy-cli": "^5.0.0",
    "rimraf": "^6.0.1"
  },
  "scripts": {
    "docs:dev": "vitepress dev",
    "docs:build": "vitepress build && rimraf docs && cpy .vitepress/dist/* docs --flat",
    "docs:preview": "vitepress preview"
  }
}